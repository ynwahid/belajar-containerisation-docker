# Create containers if not exist and start
docker compose up -d
# Stop and remove containers
docker compose down
# Start containers without creating
docker compose start
# Stop containers without removing them
docker compose stop